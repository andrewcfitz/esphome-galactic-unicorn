esphome:
  name: galactic-unicorn-color
  friendly_name: Galactic Unicorn Color

external_components:
  - source:
      type: local
      path: ../components

rp2040:
  board: rpipicow

logger:

api:
  encryption:
    key: !secret api_encryption_key

ota:
  - platform: esphome

wifi:
  ssid: !secret wifi_ssid
  password: !secret wifi_password

font:
  - file: "gfonts://Silkscreen"
    id: sign_font
    size: 8

galactic_unicorn:
  id: unicorn
  brightness: 0.5

light:
  - platform: galactic_unicorn
    id: sign_light
    name: "Sign Color"
    # ESPHome lights default restore_mode to ALWAYS_OFF, which would leave
    # this sign blank after every reboot until someone opens Home Assistant
    # and turns it back on. RESTORE_DEFAULT_ON restores the last known
    # state, defaulting to on (full white) if there is none saved yet, so
    # the sign comes back visible on its own.
    restore_mode: RESTORE_DEFAULT_ON

display:
  - platform: galactic_unicorn
    id: unicorn_display
    update_interval: 33ms
    lambda: |-
      id(sign_text)->draw(it);

text:
  - platform: galactic_unicorn
    id: sign_text
    name: "Sign Text"
    font: sign_font
    light_id: sign_light
    scroll_speed: 20
    scroll_gap: 12
